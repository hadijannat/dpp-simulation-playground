$schema: http://json-schema.org/draft-07/schema#
title: StoryDefinition
required: [code, title, steps]
properties:
  code:
    type: string
  title:
    type: string
  steps:
    type: array
    items:
      type: object
      required: [action]
      properties:
        action:
          type: string
        params:
          type: object
