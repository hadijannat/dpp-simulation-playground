# EDC API

Export OpenAPI specs with:

```bash
make openapi
```

Primary flows:

- `GET /api/v1/edc/catalog` browse catalog
- `POST /api/v1/edc/negotiations` create negotiation
- `POST /api/v1/edc/transfers` create transfer
